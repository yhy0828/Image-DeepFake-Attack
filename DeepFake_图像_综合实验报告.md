# DeepFake 图像攻防综合实验报告

> 平台：AI-SEC（https://www.rgznaq.com/arena?scenario=deepfake_image）
> 赛题：DeepFake 图像检测与对抗
> GitHub 用户名：**yhy0828**
> 提交目录：`deepfake_solutions/`

---

## 1. 题目背景与目标

DeepFake（深度伪造）图像通过生成模型对人脸进行换脸/融合，肉眼难以分辨真伪。本赛题要求分别站在**攻击方**与**防御方**两个立场：

- **攻击方**：给定一张伪造图像（`sample["image"]`，RGB uint8），通过后处理手段抹除取证痕迹，使防御方检测器误判为真实图像，同时保持视觉保真度（PSNR ≥ 24 dB、尺寸/通道不变）。
- **防御方**：给定一批带标签的训练图像（真实 + 伪造），训练一个分类器，输出 `fake_probability ∈ [0, 1]`（越接近 1 越像伪造）。

本题的核心是**取证痕迹的生成与消除**：伪造图像的换脸融合边界会留下高频振铃、重采样伪影、局部色差等统计痕迹，攻击方设法抹除，防御方设法捕捉。

---

## 2. 平台评分规则

### 2.1 攻击方评分（ASR 攻击成功率 + 保真度）

| 条件 | 得分 |
| --- | --- |
| ASR ≥ 80% 且保真达标 | 100 |
| ASR ≥ 50% 且保真达标 | 85 |
| ASR ≥ 30% 且保真达标 | 70 |
| ASR > 0 | 60 |
| ASR = 0 | 0 |

约束：`PSNR ≥ 24 dB`，输出尺寸/通道与输入一致（H×W×3）。

### 2.2 防御方评分（检出率 + 误报率）

| 条件 | 得分 |
| --- | --- |
| 检出率 ≥ 80% 且 FBR ≤ 10% | 100 |
| 检出率 ≥ 50% 且 FBR ≤ 20% | 85 |
| 检出率 ≥ 30% | 70 |
| 检测失败 | 0 |

> 注：在线评测采用 mock 评测（真实平台排行榜另有独立评测），因此在线得分与本地离线指标存在差异，详见第 6 节。

---

## 3. 攻防接口契约

### 3.1 攻击方接口

```python
def attack(sample) -> dict:
    image = sample["image"]   # RGB uint8 H×W×3
    ...
    return {"image": out}     # RGB uint8 H×W×3，与输入同尺寸
```

### 3.2 防御方接口

```python
class Defense:
    def __init__(self, C=1.0, max_iter=500, seed=42, **kwargs): ...
    def fit(self, images, labels) -> self: ...      # 训练
    def predict(self, image) -> float: ...          # 返回 P(fake|image) ∈ [0,1]
```

---

## 4. 攻击方法（6 种）

所有攻击方法均以 `attack(sample)` 为入口，类模板统一命名为 `JPEGReencodeAttack`（沿用平台 Baseline 命名，便于在线编辑面板识别）。

### 4.1 方法 1：JPEG 重编码攻击 `attack_jpeg.py`

**算法原理**
JPEG 以 8×8 分块 DCT 变换 + 量化表丢弃高频系数实现有损压缩。伪造图像在换脸融合过程中会在融合边界留下高频振铃、重采样伪影与局部纹理异常，这些痕迹在 8×8 分块 DCT 的高频系数上表现突出。对伪造图重新执行"编码 → 解码"会：

1. 打乱原图的分块边界，使原 DCT 能量分布被重排；
2. 通过量化丢弃高频取证痕迹。

质量越低压缩越强、隐匿越明显，但失真越大。本实现质量池为 `{55, 65, 75, 85}`，默认 `quality=85`。

**离线指标**：ASR=0.8958，SSIM=0.8888，PSNR=37.83 dB
**在线得分**：0（`攻击失败 · 未触发注入 qualified=false`）

---

### 4.2 方法 2：缩放-恢复攻击 `attack_resize.py`

**算法原理**
DeepFake 融合边界带有高频振铃与重采样伪影，这些精细结构对"下采样再上采样"十分敏感。将图像先缩小到中间尺寸（192/224），再用双三次插值（BICUBIC）放大回原尺寸（256），等价于一次低通平滑重采样：

- 下采样抑制高频振铃与局部纹理噪声；
- 上采样插值抹平原融合边界的锐利边缘。

相比 JPEG 不引入分块伪影，视觉上更接近"轻微模糊"，但仍能有效降低依赖高频特征的检测器置信度。

**离线指标**：ASR=0.8333，SSIM=0.9568，PSNR=38.26 dB
**在线得分**：83（`攻方成功 · 注入生效 qualified=true`）

---

### 4.3 方法 3：模糊-锐化攻击 `attack_blur.py`

**算法原理**
DeepFake 检测器的频域特征依赖 SRM 高通噪声残差（本质是对图像做高通滤波提取边缘/噪声），空域特征依赖水平/垂直梯度统计。伪造图像的融合边界振铃与内部纹理噪声会使这些高通响应异常偏高。

本攻击采用"先模糊后锐化"：

1. 轻度高斯模糊（σ≈1.2）抑制高频噪声与振铃，降低 SRM 残差能量与梯度；
2. UnsharpMask 重新锐化，恢复主体边缘视觉清晰度，避免整体变糊。

**离线指标**：ASR=1.0000，SSIM=0.9069，PSNR=33.83 dB
**在线得分**：0

---

### 4.4 方法 4：颜色洗白攻击 `attack_color.py`

**算法原理**
换脸 DeepFake 常在被换脸区域引入轻微色差（肤色/光照不一致），像素统计特征（每通道均值/标准差/极差/分位差）与颜色统计特征会捕捉到这种颜色分布偏移。本攻击通过轻微非线性颜色空间变换"洗白"色差：

1. **Gamma 校正**（γ≈0.9）：改变整体亮度响应曲线，压缩/拉伸中间调，抹平局部色偏；
2. **饱和度调整**（s≈1.1）：将像素向灰度方向或远离灰度方向拉伸，弱化肤色纹理差异；
3. **色温偏移**（t≈0，R 通道 +t、B 通道 -t）：模拟白平衡微调。

变换幅度轻微，SSIM/PSNR 保真度很好，但对频域检测器基本无效。

**离线指标**：ASR=0.0000，SSIM=0.9898，PSNR=28.31 dB
**在线得分**：0（`qualified=true`，未触发注入）

---

### 4.5 方法 5：频域洗白攻击 `attack_frequency.py`（本实验最优）

**算法原理**
DeepFake 检测器（frequency_stat）的核心特征之一是全局 FFT 幅度谱高频带统计、SRM 高通残差，以及 8×8 分块 DCT 高频系数，本质都在衡量图像的"高频能量"。伪造图的融合边界振铃与内部高频噪声会让高频能量异常偏高。

本攻击直接在频域做平滑低通滤波：

1. 对每个通道做 2D FFT，频谱中心化（`fftshift`）；
2. 用 Sigmoid 软掩码抑制高频分量：

   ```
   mask(r) = 1 / (1 + exp((r - cutoff) * 12))
   ```

   其中 r 为归一化频率半径，cutoff≈0.30，过渡带平滑避免振铃；
3. 用 mask 对频谱加权后逆变换回空域，并按 strength=0.5 与原图线性混合：

   ```
   out = (1 - strength) * img + strength * lowpass(img)
   ```

由于低频（人脸轮廓/肤色主体）几乎不变，视觉保真度与 PSNR 都保持得很好。

**离线指标**：ASR=1.0000，SSIM=0.9686，PSNR=36.70 dB
**在线得分**：100（满分）

---

### 4.6 方法 6：隐藏组合攻击 `attack_composite.py`

**算法原理**
单一攻击各有侧重，也都可能被针对性防御检出残留痕迹。组合攻击从攻击池中随机抽取 3 步串联执行，形成随机、不可预测的后处理链：

```
jpeg -> resize_recover -> blur_sharpen -> color_laundering -> ...
```

核心思想是"分而治之 + 叠加隐匿"：

- JPEG 破坏 8×8 分块 DCT 高频能量分布；
- 缩放-恢复抑制重采样/振铃伪影；
- 模糊-锐化削弱 SRM 噪声残差与梯度统计；
- 颜色洗白抹平颜色统计差异；
- 频域洗白直接压制 FFT 高频带。

多步串联使各类检测特征被逐层削弱，随机链结构对防御方不可复现。代价是多步叠加会累积失真，需控制步数与强度以通过保真度门槛。

**离线指标**：ASR=1.0000，SSIM=0.9006，PSNR=32.58 dB
**在线得分**：0（`qualified=false`）

---

## 5. 防御方法（3 种）

三种防御均采用 **"特征提取 + StandardScaler 标准化 + 逻辑回归 + sigmoid"** 的经典机器学习范式，仅在特征维度/来源上不同。

### 5.1 方法 1：频域统计检测器 `defense_frequency.py`

**算法原理**（31 维特征，由四部分构成）

1. **SRM 高通噪声残差统计（12 维）**：用 3 个标准 SRM（Spatial Rich Model）高通滤波器对灰度图卷积，提取噪声/边缘残差，各取均值/标准差/绝对均值/99%-1% 分位差。伪造图的融合边界振铃会让高通残差异常偏高。
2. **8×8 分块 DCT 能量谱（6 维）**：对灰度图做 8×8 分块 2D-DCT，累加各块幅度谱，取 5 个代表性系数（低频 0/1/9 与高频 56/63）的 log1p 能量，以及高频/低频能量比。
3. **全局 FFT 幅度谱频带统计（4 维）**：对整图 2D FFT，取低频带（半径<15%）与高频带（半径>40%）的 log1p 幅度均值与标准差。
4. **每通道颜色统计（9 维）**：RGB 三通道的均值/标准差/极差。

**离线指标（干净测试集）**：DR=1.0000，FBR=0.0000，ACC=1.0000，AUC=1.0000
**在线得分**：64（`防御成功 · 全部拦截 · FBR 0.0%`）

---

### 5.2 方法 2：像素统计检测器 `defense_pixel.py`

**算法原理**（15 维空域特征）

1. **每通道颜色统计（12 维）**：RGB 三通道各取均值/标准差/极差/95%-5% 分位差，刻画换脸区域色差与动态范围异常。
2. **灰度纹理统计（3 维）**：灰度图水平梯度绝对均值、垂直梯度绝对均值、灰度标准差，刻画融合边界锐利振铃与内部噪声。

空域特征实现简单、计算快，对频域后处理（JPEG/低通）相对稳健，是频域检测器的互补。

**离线指标（干净测试集）**：DR=1.0000，FBR=0.0000，AUC=1.0000
**在线得分**：74（`防御成功 · 全部拦截 · FBR 0%`）

---

### 5.3 方法 3：SRM 噪声残差检测器 `defense_srm.py`

**算法原理**（12 维纯 SRM 特征）

SRM（Spatial Rich Model，空间富模型）是数字图像取证经典工具：用一族高通线性滤波器对图像卷积得到"噪声残差"，再统计残差分布。真实自然图像的噪声残差遵循特定统计规律，而篡改/伪造（换脸融合）会破坏该规律。

本检测器仅使用 3 个标准 SRM 高通核（5×5 二阶、5×5 一阶、3×3 拉普拉斯）卷积灰度图，每个残差图取 4 个统计量（均值/标准差/绝对均值/99%-1% 分位差），共 3×4=12 维特征，送入 StandardScaler + 逻辑回归。维度更低、更聚焦噪声残差，对"只做颜色/亮度级攻击"的对抗样本有互补检出能力。

> **在线适配**：纯 SRM 版本在线提交时出现 `ValueError: Input contains NaN`（平台 `roc_auc_score` 遇 NaN），因此在线提交版改为 **SRM 12 维 + RGB 颜色 9 维** 混合，并对特征与 `decision_function` 使用 `np.nan_to_num`。

**离线指标（干净测试集）**：DR=1.0000，FBR=0.0000，AUC=1.0000
**在线得分**：64（SRM+颜色混合版）

---

## 6. 实验结果汇总

### 6.1 攻击方法结果表

| 方法 | 文件 | 离线 ASR | 离线 SSIM | 离线 PSNR | 在线得分 |
| --- | --- | --- | --- | --- | --- |
| JPEG 重编码 | attack_jpeg.py | 0.8958 | 0.8888 | 37.83 dB | 0 |
| 缩放-恢复 | attack_resize.py | 0.8333 | 0.9568 | 38.26 dB | **83** |
| 模糊-锐化 | attack_blur.py | 1.0000 | 0.9069 | 33.83 dB | 0 |
| 颜色洗白 | attack_color.py | 0.0000 | 0.9898 | 28.31 dB | 0 |
| 频域洗白 | attack_frequency.py | 1.0000 | 0.9686 | 36.70 dB | **100** |
| 隐藏组合 | attack_composite.py | 1.0000 | 0.9006 | 32.58 dB | 0 |

### 6.2 防御方法结果表

| 方法 | 文件 | 离线 DR | 离线 FBR | 离线 AUC | 在线得分 |
| --- | --- | --- | --- | --- | --- |
| 频域统计 | defense_frequency.py | 1.0000 | 0.0000 | 1.0000 | 64 |
| 像素统计 | defense_pixel.py | 1.0000 | 0.0000 | 1.0000 | **74** |
| SRM（+颜色） | defense_srm.py | 1.0000 | 0.0000 | 1.0000 | 64 |

---

## 7. 在线与离线差异分析

1. **本地 ASR=1.0 的方法（blur/composite/jpeg）在线多得分 0**：在线 mock 评测对"保真度门槛"判定更严格，且 mock 注入/评测逻辑与本地 `test.py` 不完全一致，导致本地逃逸成功的攻击在线被判"未触发注入"或 `qualified=false`。
2. **频域洗白最稳定**：本地 ASR=1.0 且 PSNR 高，在线也得满分 100，说明直接压制高频取证痕迹对真实检测器最有效。
3. **防御方在线分数偏低（64/74）**：在线 mock 防御评测可能存在更难的对抗样本或不同阈值判定，导致本地干净集上 100% 的检测器在线仅得 64~74。
4. **结论**：本地离线指标是算法有效性的参考，但**在线得分才是平台最终判定**，需以在线为准。

---

## 8. 平台约束与坑点记录

1. **禁止 `from __future__ import annotations`**：平台将防御代码合并到已有 `defense.py` 内容之后，`from __future__` 必须位于文件开头，否则报 `SyntaxError`。在线提交版已删除该导入。
2. **特征提取必须 NaN-safe**：纯 SRM 版在线出现 `ValueError: Input contains NaN`，需用 `np.nan_to_num` 兜底，或改用 SRM+颜色混合特征。
3. **保真度门槛**：攻击需满足 `PSNR ≥ 24 dB` 且尺寸/通道不变，否则 `qualified=false`。
4. **在线编辑器 ref 过期**：每次重新进入对战需重新 `snapshot` 动态抓取 `textbox [eN]` 与执行按钮 ref。

---

## 9. 文件清单

| 文件 | 说明 |
| --- | --- |
| attack_jpeg.py | 攻击 1：JPEG 重编码 |
| attack_resize.py | 攻击 2：缩放-恢复 |
| attack_blur.py | 攻击 3：模糊-锐化 |
| attack_color.py | 攻击 4：颜色洗白 |
| attack_frequency.py | 攻击 5：频域洗白（最优） |
| attack_composite.py | 攻击 6：隐藏组合 |
| defense_frequency.py | 防御 1：频域统计（31 维） |
| defense_pixel.py | 防御 2：像素统计（15 维） |
| defense_srm.py | 防御 3：SRM 噪声残差（12 维，在线为 SRM+颜色） |
| local_test.py | 本地离线综合评测脚本 |
| DeepFake_图像_综合实验报告.md | 本报告 |

---

## 10. 结论

本实验围绕 DeepFake 图像赛题，实现了 **6 种攻击方法** 与 **3 种防御方法**，并在平台上完成在线编辑验证。

- **攻击侧**：频域洗白（FFT 低通 + Sigmoid 软掩码）是唯一同时满足"高 ASR + 高保真 + 在线满分"的方法，说明**直接压制高频取证痕迹**是逃逸频域/像素检测器的最有效手段；缩放-恢复在在线评测中也取得 83 分。
- **防御侧**：像素统计检测器在线得分最高（74），频域统计与 SRM 检测器各得 64；三者离线均可达 DR=1.0/FBR=0.0，说明经典"特征 + 逻辑回归"范式在干净数据上足够区分真伪，但在线评测更难、得分收敛于 64~74。
- **经验**：取证对抗的本质是"高频痕迹"的生成与消除博弈，攻击应优先在频域下手，防御应兼顾频域、空域与颜色多域特征以提升鲁棒性。

---

*提交人 GitHub 用户名：yhy0828*
